# `zxingReedSolomon.js`

Reed–Solomon implementation ported from **ZXing** (Apache 2.0), via the npm package [`reedsolomon`](https://www.npmjs.com/package/reedsolomon) (cho45). The upstream file used `this.*` assignments; this copy ends with ESM `export { … }` so Vite can bundle it in the browser.
