# Python: training + inference
# TensorFlow: use Python 3.10–3.12 for pip install tensorflow (CNN trainer).
# On Python 3.13+, use: python backend/train_embed_mlp.py (sklearn only).
pillow>=10.0.0
opencv-python-headless>=4.8.0,<4.13.0
numpy>=1.24.0
scikit-learn>=1.3.0
scikit-image>=0.22.0
joblib>=1.3.0
tensorflow>=2.15.0,<2.18.0; python_version < "3.13"
