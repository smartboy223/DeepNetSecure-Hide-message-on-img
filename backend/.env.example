# DeepNetSecure backend — optional overrides
# Copy to backend/.env.local if you need them.

# HTTP port for node backend/server.js (default 4006).
# Use this name — do NOT set generic PORT=... here; Windows often has PORT set globally.
# DEEPNET_PORT=4006

# Python for analyze_image / inference (optional)
# PYTHON_CMD=py -3
